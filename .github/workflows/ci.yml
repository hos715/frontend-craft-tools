name: CI

on:
  push:
    branches: [ main, ci-badge ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run basic validation
        run: |
          echo "Frontend Craft Tools CI workflow"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Branch: $GITHUB_REF"
