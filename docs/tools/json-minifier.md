# JSON Minifier

Client-side JSON minifier with drag-and-drop support. Drops whitespace and serializes JSON for minimal size.

Usage:

1. Open the tool from the site.
2. Drag or select a `.json` file.
3. Download the minified JSON.
